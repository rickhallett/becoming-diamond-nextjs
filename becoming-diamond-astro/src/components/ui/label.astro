---
interface Props {
  for?: string;
  className?: string;
}

const {
  for: htmlFor,
  className = ""
} = Astro.props;
---

<label
  for={htmlFor}
  class={`
    block text-sm font-medium text-gray-300 mb-2
    ${className}
  `.trim()}
>
  <slot />
</label>
