---
import Layout from '../layouts/Layout.astro';
import LandingPage from '../components/landing/LandingPage';
---

<Layout
  title="Becoming Diamond - Transform Pressure into Power"
  description="Master human presence under pressure. Regulate your nervous system. Rewire your identity. Lead with unshakable clarity—even when everything around you is chaos."
>
  <LandingPage client:load />
</Layout>

<style is:global>
  :root {
    --primary: #4fc3f7;
    --secondary: #1a1a2e;
  }

  .text-primary {
    color: var(--primary);
  }

  .bg-primary {
    background-color: var(--primary);
  }

  .border-primary {
    border-color: var(--primary);
  }

  .bg-secondary {
    background-color: var(--secondary);
  }
</style>
